<nav class="navbar navbar-expand-lg navbar-dark bg-dark sticky-top">
    <!-- remove navbar-dark bg-dark once you get to CSS -->
    <div class="container">
        <a class="navbar-brand fw-semibold" href="/">
            <i class="fas fa-film me-2"></i> MovieSearch
        </a>

        <button
            class="navbar-toggler"
            type="button"
            data-bs-toggle="collapse"
            data-bs-target="#navbarContent"
            aria-controls="navbarContent"
            aria-expanded="false"
            aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarContent">
            <ul class="navbar-nav me-auto">
                <% if (currentUser) { %>
                <li class="nav-item">
                    <a class="nav-link" href="/movies">
                        <i class="fas fa-bookmark me-1"></i> Saved Movies
                    </a>
                </li>
                <% } %>
            </ul>

            <ul class="navbar-nav">
                <% if (currentUser) { %>
                <li class="nav-item">
                    <form action="/api/users/logout" method="POST" class="d-inline">
                        <button type="submit" class="nav-link btn btn-link p-0">
                            <i class="fas fa-arrow-right-from-bracket me-1"></i> Logout
                        </button>
                    </form>
                </li>
                <% } else { %>
                <li class="nav-item">
                    <a class="nav-link" href="/login">
                        <i class="fas fa-circle-user me-1"></i> Sign In / Sign Up
                    </a>
                </li>
                <% } %>
            </ul>
        </div>

    </div>
</nav>